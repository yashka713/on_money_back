en:
  user:
    errors:
      forbidden: You are not authorized to access this page.

  account:
    errors:
      rate:
        empty: Currency exchange rate can't be 0, string or empty
      accounts_match: Accounts matches

  transactions:
    errors:
      transfer_not_allowed: Allowed operation only between two accounts
      profit_not_allowed: Allowed operation where profitable is Account and chargeable is Category
      charge_not_allowed: Allowed operation where profitable is Category and chargeable is Account
      operation_not_allowed: Operation between two Categories don't allowed
      unknown_operation_type: Unknown operation type
      not_owner: "You are not owner %{chargeable} or %{profitable}"
      attributes_hidden: One of the attributes is not allowed for this operation

  category:
    errors:
      type_changed: Category type can't be changed
      max_amount_riched: Maximum amount of categoris reached

  currency:
    errors:
      unknown_currency: Unknown currency

  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              blank: Must be 6 or more characters, at least one upper case and one number.
              format: Must be 6 or more characters, at least one upper case and one number.

  errors:
    messages:
      invalid_date: "is not a valid date"
      invalid_time: "is not a valid time"
      invalid_datetime: "is not a valid datetime"
      is_at: "must be at %{restriction}"
      before: "must be before %{restriction}"
      on_or_before: "must be on or before %{restriction}"
      after: "must be after %{restriction}"
      on_or_after: "must be on or after %{restriction}"

  number:
    currency:
      format:
        delimiter: ","
        separator: "."
